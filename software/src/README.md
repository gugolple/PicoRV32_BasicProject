#Clasic and basic compilation of the source files

This is the original compilation way. A lot simpler but without all the
advantages of having the autotools logic.

This will create all expected outputs.
